---
interface Props {
  class?: string;
  id?: string;
}

const { class: className = "", id } = Astro.props;
---

<section id={id} class={`relative px-6 sm:px-8 lg:px-12 max-w-7xl mx-auto w-full ${className}`}>
  <slot />
</section>
