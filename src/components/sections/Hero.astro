---
import Section from '../ui/Section.astro';
import Button from '../ui/Button.astro';
import { FadeIn } from '../ui/FadeIn';
---

<Section class="pt-32 pb-12 md:pt-40 md:pb-32 lg:pt-48 lg:pb-40 relative overflow-hidden">
  <!-- Animated gradient background -->
  <div class="absolute inset-0 animated-gradient opacity-40"></div>
  
  <div class="relative z-10 grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 items-center">
    
    <!-- Left Column: Content -->
    <div class="text-left relative z-20">
      <FadeIn client:load delay={0.1}>
        <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-white/5 border border-white/10 mb-8 backdrop-blur-sm">
          <span class="w-2 h-2 rounded-full bg-lumo-brand animate-pulse"></span>
          <span class="text-xs font-medium text-lumo-brand tracking-wide uppercase">Inteligência Financeira</span>
        </div>
        
        <h1 class="heading-xl text-white mb-6 text-balance leading-[1.1] text-glow-subtle">
          Agentes de IA<br />
          <span class="text-lumo-brand text-glow-brand">no seu Omie</span>
        </h1>
      </FadeIn>
      
      <FadeIn client:load delay={0.2}>
        <p class="body-lg text-lumo-text-secondary mb-10 max-w-xl text-balance leading-relaxed">
          Delegue a execução repetitiva para agentes autônomos. Liberte seu time financeiro para focar em estratégia, não em digitação.
        </p>
      </FadeIn>
      
      <FadeIn client:load delay={0.3}>
        <form class="contact-form flex flex-col sm:flex-row gap-4 max-w-md" onsubmit="event.preventDefault();">
          <input 
            type="email" 
            name="email"
            required
            placeholder="Seu e-mail corporativo" 
            class="flex-1 bg-white/5 border border-white/10 rounded-lg px-4 py-3.5 text-white placeholder:text-gray-500 focus:outline-none focus:border-lumo-brand/50 focus:ring-1 focus:ring-lumo-brand/50 transition-all backdrop-blur-sm"
          />
          <Button variant="primary" type="submit" class="!px-6 !py-3.5 whitespace-nowrap">
            Saiba mais
          </Button>
        </form>
        <p class="text-xs text-lumo-text-muted mt-4 ml-1" data-form-message>
          Junte-se a líderes financeiros de empresas high-growth.
        </p>
      </FadeIn>
    </div>

    <!-- Right Column: Visual -->
    <div class="hidden lg:flex relative lg:absolute lg:right-0 lg:top-1/2 lg:-translate-y-1/2 lg:w-[55%] h-full items-center justify-center lg:justify-end pointer-events-none">
       <FadeIn client:load delay={0.4} className="relative w-full aspect-square max-w-[600px] lg:max-w-[800px]">
          <!-- Main Visual -->
          <div class="absolute inset-0 bg-gradient-to-l from-lumo-brand/10 via-transparent to-transparent opacity-30 blur-3xl -z-10"></div>
          
          <!-- Use mask-image to fade edges perfectly into black -->
          <img 
            src="/assets/hero-visual.png" 
            alt="Lumo AI Agent" 
            class="w-full h-full object-contain drop-shadow-[0_0_80px_rgba(255,200,44,0.1)] animate-float-slow mix-blend-screen"
            style="mask-image: radial-gradient(circle at center, black 40%, transparent 70%); -webkit-mask-image: radial-gradient(circle at center, black 40%, transparent 70%);"
          />
          
          <!-- Orbiting particles (CSS only) -->
          <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[100%] h-[100%] rounded-full border border-white/5 animate-spin-slow-reverse"></div>
          <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[70%] h-[70%] rounded-full border border-dashed border-lumo-brand/10 animate-spin-slow"></div>
       </FadeIn>
    </div>
    
  </div>
</Section>

<style>
  .animate-float-slow {
    animation: float 8s ease-in-out infinite;
  }
  .animate-spin-slow {
    animation: spin 20s linear infinite;
  }
  .animate-spin-slow-reverse {
    animation: spin 25s linear infinite reverse;
  }
  
  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-20px); }
  }
  @keyframes spin {
    from { transform: translate(-50%, -50%) rotate(0deg); }
    to { transform: translate(-50%, -50%) rotate(360deg); }
  }
</style>
